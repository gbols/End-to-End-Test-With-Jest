import { Query } from './query/Query';
import { Mutation } from './mutation'
import { Todo, User } from './QueryHelpers';

const resolvers = {
  Query,
  Mutation,
  Todo,
  User
}


export default resolvers;